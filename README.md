# Launching
So far we just have a basic test server and client.
- First run a server: `cargo run -- --server`
- Then run a client: `cargo run`
- Do a search for `CommandLineArgs` in the codebase for other command line args.

multiplayer:
- [x] jitter buffer

abilites:
- [x] dash

